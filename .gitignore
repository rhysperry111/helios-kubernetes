# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Sensitive files passed between steps
*.autogen.*

# Generated per-step vars (regenerate with `make generate`)
helios.auto.tfvars
helios.auto.yaml

# Sensitive secrets (never commit)
secrets.auto.tfvars
