# /etc/hosts - Managed by Ansible. Do not edit manually.
127.0.0.1   localhost
::1         localhost ip6-localhost ip6-loopback

# Kubernetes control plane VIP
{{ k8s_vip }}  api.{{ k8s_domain }} kubernetes.{{ k8s_domain }}

# Cluster nodes
{% for host in groups['all'] %}
{{ hostvars[host].ansible_host }}  {{ hostvars[host].inventory_hostname }}.{{ k8s_domain }}  {{ hostvars[host].inventory_hostname }}
{% endfor %}
